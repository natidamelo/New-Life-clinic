const { MongoClient } = require('mongodb'); async function run() { const client = new MongoClient('mongodb+srv://kinfenati7_db_user:Natkinfe2325@cluster0.smcnulu.mongodb.net/clinic-cms?retryWrites=true&w=majority&appName=Cluster0'); await client.connect(); const db = client.db('clinic-cms'); const p = await db.collection('patients').find({ status: { $in: ['waiting', 'queue', 'in_queue', 'pending'] } }).toArray(); console.log(p.map(x => ({ id: x.patientId, hasPaidCardFee: x.hasPaidCardFee, hasUnpaidCardInvoice: x.hasUnpaidCardInvoice, cardStatus: x.cardStatus }))); await client.close(); } run().catch(console.error);