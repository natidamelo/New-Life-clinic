{
  "installCommand": "cd frontend && npm install",
  "buildCommand": "cd frontend && NODE_OPTIONS=--max-old-space-size=7168 npx vite build",
  "outputDirectory": "frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
